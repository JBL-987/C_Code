#include <stdio.h>
int main() {
	float m , n , o, p, q;
	scanf("%f %f %f %f", &m, &n, &o, &p);
	q = 2 * m + 2 * n + o * 2 + p;
	printf("%.2f\n", q);
	scanf("%f %f %f %f", &m, &n, &o, &p);
	q = 2 * m + 2 * n + o * 2 + p;
	printf("%.2f\n", q);
	scanf("%f %f %f %f", &m, &n, &o, &p);
	q = 2 * m + 2 * n + o * 2 + p;
	printf("%.2f\n", q);
	return 0;
}