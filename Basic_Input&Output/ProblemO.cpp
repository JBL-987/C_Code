#include <stdio.h>

int main() {
    char str[100];
    char str1[100];
    char str2[100];
    scanf("%s %s %s", str, str1, str2);
    printf("%s %s %s\n", str2, str1, str);
    return 0;
}

